# ----
# Run in the Termux (terminal emulator) command-line.
# ----

# -> $PREFIX/etc/bash.bashrc
export PS1='\n[This is a command-line shell interpreter, that displays in a terminal application.]\n['"(current history list number is 0? supportive prompt line)"']\n\#. \V \h \$ '
# n...